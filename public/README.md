# Public Assets Organization

โครงสร้างโฟลเดอร์ `/public` ที่จัดระเบียบแล้ว สำหรับจัดเก็บไฟล์สื่อต่างๆ

## 📁 โครงสร้างโฟลเดอร์

```
/public
├── /assets                 # ไฟล์ทรัพย์สินต่างๆ
│   └── /icons             # ไอคอน SVG (file, globe, window, next, vercel)
├── /slideshow            # ส่วนแสดงภาพสไลด์โชว์
│   ├── /images          # ภาพ slideshow (photobooth_*.webp)
│   └── /videos          # วิดีโอ slideshow (file_photobooth_preview_*.mp4)
├── /models               # รูปแบบผลิตภัณฑ์
│   └── /images          # ภาพ product models (product-*.webp)
├── robots.txt           # SEO: Robot crawling rules
├── sitemap.xml          # SEO: Sitemap index
└── sitemap-0.xml        # SEO: Sitemap pages
```

## 📝 คำแนะนำการใช้งาน

### 1. Slideshow Section (`/slideshow`)

**เพิ่มภาพ slideshow ใหม่:**

1. วางไฟล์ภาพใน `/public/slideshow/images/`
2. ตั้งชื่อไฟล์ตามรูปแบบ: `photobooth_XX.webp` (XX = หมายเลข เช่น 25, 26, 27...)
3. แก้ไข `components/slideshow-section.tsx`:
   - เปลี่ยนค่า `MAX_IMAGES` ให้ตรงกับจำนวนภาพทั้งหมด

**ตัวอย่าง:**

```tsx
// ถ้ามีภาพ 30 รูป (photobooth_1.webp ถึง photobooth_30.webp)
const MAX_IMAGES = 30;
```

**เพิ่มวิดีโอ slideshow:**

1. วางไฟล์วิดีโอใน `/public/slideshow/videos/`
2. ตั้งชื่อไฟล์ตามรูปแบบ: `file_photobooth_preview_X.mp4`
3. แก้ไข `components/video-gallery.tsx` ใน array `ALL_VIDEOS`:
   - เพิ่ม object ใหม่พร้อม title, description, และ category

**ตัวอย่าง:**

```tsx
{
  id: 7,
  src: "/slideshow/videos/file_photobooth_preview_6.mp4",
  title: "New Memory",
  description: "ความทรงจำใหม่",
  category: "wedding",
}
```

### 2. Product Models (`/models`)

**เพิ่ม/แก้ไขรูปผลิตภัณฑ์:**

1. วางไฟล์ภาพใน `/public/models/images/`
2. แก้ไข `components/product-section.tsx` ใน array `products`
3. ใช้ path: `/models/images/product-XX-768x768.webp`

## 🎨 รูปแบบไฟล์ที่แนะนำ

| ประเภท         | รูปแบบไฟล์ | ขนาดที่แนะนำ | หมายเหตุ                 |
| -------------- | ---------- | ------------ | ------------------------ |
| Slideshow      | WebP       | 1920x1440px  | ประหยัดพื้นที่เก็บข้อมูล |
| Product Models | WebP       | 768x768px    | รูปสี่เหลี่ยมจัตุรัส     |
| Icons          | SVG        | Vector       | สำหรับไอคอนต่างๆ         |

## 🔍 SEO Files

- **robots.txt**: กำหนดกรรมพันธุ์สำหรับ search engine crawlers
- **sitemap.xml**: ไฟล์ sitemap หลักที่ชี้ไปยัง sitemap ย่อย
- **sitemap-0.xml**: Sitemap สำหรับหน้าต่างๆ ในเว็บไซต์

## ⚠️ หมายเหตุสำคัญ

1. **อย่าวางไฟล์โดยตรงใน `/public` root** - ใช้โฟลเดอร์ย่อยเท่านั้น
2. **ตั้งชื่อไฟล์ให้สื่อความหมาย** - เช่น `wedding-luxury-hotel.jpg` แทน `IMG_001.jpg`
3. **ใช้ WebP สำหรับภาพใหม่** - ประหยัดพื้นที่และโหลดเร็วกว่า JPG/PNG
4. **เพิ่ม alt text ที่ดี** - ช่วยทั้ง SEO และ accessibility
5. **ตรวจสอบขนาดไฟล์** - ควรบีบอัดภาพก่อนใช้งานเพื่อประสิทธิภาพที่ดีกว่า

## 📚 Files ที่สัมพันธ์

- `/components/slideshow-section.tsx` - Component แสดงผล slideshow (ภาพ)
- `/components/video-gallery.tsx` - Component แสดงผล video slideshow
- `/components/product-section.tsx` - Component แสดงผล products

---

**อัพเดทล่าสุด:** 2026-02-06
**จัดระเบียบโดย:** Antigravity AI Assistant
