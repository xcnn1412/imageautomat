# next-auth@5 beta พิน nodemailer ^7 เป็น optional peer แต่โปรเจกต์ใช้ nodemailer 8
# (next-auth ใช้ Google OAuth ไม่ใช่ email provider เลยไม่กระทบ) — ให้ npm install รันได้โดยไม่ต้องใส่ flag
legacy-peer-deps=true
